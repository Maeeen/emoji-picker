export struct EmojiSkinToneModel {
    skin-tone: int,
    code: string,
    image: image
}

export struct EmojiModel {
    name: string,
    code: string,
    image: image,
    skin-tones: [EmojiSkinToneModel]
}

export struct EmojiGroupModel {
    title: string,
    image: image,
    emojis: [EmojiModel]
}